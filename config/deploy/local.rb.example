# Copy to local.rb (gitignored) only if you need extra SSH options.
# Default is the deploy user with your Mac keys from ~/.ssh — no PEM.
# set :ssh_options, {
#   user: "deploy",
#   auth_methods: %w[publickey]
# }
