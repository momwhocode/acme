# Installed to /etc/cron.d/acme by deploy:update_crontab.
# Daily FX snapshot after the ECB publish. Source is credentials fx.source.
15 6 * * * deploy /bin/bash -lc 'cd /var/www/acme/current && /usr/local/bin/bundle exec rails fx:sync'
