# April renderers

Shared **constants** and **Storybook playground arg helpers** (`resolve*PlaygroundArgs`).

- App UI lives in `app/frontend/april/components/` (React).
- Demo table rows: `app/frontend/stories/_helpers/tableStory.js`.
- Do not add HTML-string `april*` renderers here.
